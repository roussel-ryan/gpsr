name: gpsr
channels:
  - conda-forge
dependencies:
  - python=3.12
  - pip
  - pip:
    - numpy
    - matplotlib>=3.3.0
    - scipy
    - ipykernel
    - tqdm
    - torch
    - lightning
    - tensorboard
    - cheetah-accelerator>=0.7.2
